package io.github.yasenia.pricing.common;

import java.math.BigDecimal;

public class Numbers {

    // The error used for Matchers.closeTo(operand, error).
    public static final BigDecimal DELTA = BigDecimal.valueOf(0.001);
}
